# ── Core (all platforms) ──────────────────────────────────────────────────────
flask>=3.0
psutil>=5.9; sys_platform == "win32"
pywin32>=306; sys_platform == "win32"
win10toast>=0.9; sys_platform == "win32"

# ── Windows only ─────────────────────────────────────────────────────────────
# Installed via platform markers above:
# pywin32        # win32gui, win32process
# psutil         # process name lookup / background process management
# win10toast     # desktop notifications

# ── macOS only ───────────────────────────────────────────────────────────────
# pyobjc-framework-Cocoa>=10.0   # AppKit / NSWorkspace

# ── Linux: xdotool installed via apt, no pip package needed ──────────────────
# sudo apt install xdotool notify-send
